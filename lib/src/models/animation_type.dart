/// Animation type for loading effect
enum AnimationType {
  /// Classic left-to-right shimmer effect
  shimmer,

  /// Pulsing fade in/out effect
  pulse,

  /// Combined shimmer and pulse
  combined,
}
